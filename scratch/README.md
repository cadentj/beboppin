# Scratch

Temporary workspace for one-off calculations, local tooling, and disposable artifacts.

If extra packages are needed, install them in `scratch/.venv` or another subdirectory inside `scratch`.
