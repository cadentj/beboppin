{
  "name": "clipper",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm clean && vite build --mode content && vite build --mode popup",
    "dev": "pnpm build && concurrently -k -n content,popup \"vite build --watch --mode content\" \"vite build --watch --mode popup\"",
    "dev:ext": "web-ext run --target=chromium --source-dir=dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "concurrently": "^9.2.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "web-ext": "^10.1.0"
  }
}
